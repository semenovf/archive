--************************************************************
--* Generated automatically by `gbs'
--* Command: `gbs pro --create --name=pfs-cli --type=shared-lib'
--* Date:    Thu Aug 20 12:14:50 2015
--************************************************************

kind          "SharedLib"
language      "C++"
targetname    "pfs-cli"
defines       {  }
includedirs   { "../../include" }
libdirs       { "../../../.build" }
files         { "../../src/pfs-cli/*.c", "../../src/pfs-cli/*.cpp", "../../include/**.hpp", "../../include/**.h" }

configuration "debug"
    flags        { "Symbols" }
    defines      { "DEBUG" }
    objdir       "../../../.build/pfs-cli/pfs-cli/debug"
    targetdir    "../../../.build"
    targetsuffix "-d"
    links        {  }

configuration "release"
    flags        {  }
    defines      { "NDEBUG" }
    objdir       "../../../.build/pfs-cli/pfs-cli/release"
    targetdir    "../../../.build"
    links        {  }

filter "action:gmake"
    include(os.getenv("GBS_HOME") .. "/premake/filter_action_gmake_install")
