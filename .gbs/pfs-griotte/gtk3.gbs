filter "action:gmake"
    buildoptions {
	  "`pkg-config --cflags cairo`"
	, "`pkg-config --cflags gtk+-3.0`" 
    }

    linkoptions  {
	  "`pkg-config --libs cairo`"
	, "`pkg-config --libs gtk+-3.0`" 
    }
