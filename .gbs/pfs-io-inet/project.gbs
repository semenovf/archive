--************************************************************
--* Generated automatically by `gbs'
--* Command: `gbs project --create --name=pfs-io-inet --type=shared-lib'
--* Date:    Thu Aug 20 14:33:02 2015
--************************************************************

kind          "SharedLib"
language      "C++"
targetname    "pfs-io-inet"
defines       {  }
includedirs   {   "../../include"
                , "../../../pfs/include" 
                , "../../../pfs-sys/include" }
libdirs       { "../../../.build" }
files         { "../../src/pfs-io-inet/*.c", "../../src/pfs-io-inet/*.cpp", "../../include/**.hpp", "../../include/**.h" }

configuration "debug"
    flags        { "FatalWarnings", "Symbols" }
    defines      { "DEBUG" }
    objdir       "../../../.build/pfs-io/pfs-io-inet/debug"
    targetdir    "../../../.build"
    targetsuffix "-d"
    links        {  }

configuration "release"
    flags        { "FatalWarnings" }
    defines      { "NDEBUG" }
    objdir       "../../../.build/pfs-io/pfs-io-inet/release"
    targetdir    "../../../.build"
    links        {  }

filter "action:gmake"
    include(os.getenv("GBS_HOME") .. "/premake/filter_action_gmake_install")
