--************************************************************
--* Generated automatically by `gbs'
--* Command: `gbs prj --create --type=test --depends=pfs --name=test-cli'
--* Date:    Wed Dec 14 12:05:22 2016
--************************************************************

kind          "ConsoleApp"
language      "C++"
targetname    "test-cli"
defines       {  }
includedirs   { "../../include" }
libdirs       { "../../../.build" }
files         { "../../tests/test-cli/*.c", "../../tests/test-cli/*.cpp" }

configuration "debug"
    flags        { "FatalWarnings", "Symbols" }
    defines      { "DEBUG" }
    objdir       "../../../.build/pfs/test-cli/debug"
    targetdir    "../../../.build/tests"
    targetsuffix "-d"
    links        {  }

configuration "release"
    flags        { "FatalWarnings" }
    defines      { "NDEBUG" }
    objdir       "../../../.build/pfs/test-cli/release"
    targetdir    "../../../.build/tests"
    links        {  }

dependson { "pfs" }


filter "action:gmake"
    buildoptions {}
    linkoptions  {}

include(os.getenv("GBS_HOME") .. "/premake/filter_action_gmake_install")

filter { "debug", "action:gmake" }
    linkoptions  { "-rdynamic" }

