package JQ::IO;

# !obsolete
# use JQ::Exception::IO instead
use Exception::Class (
    __PACKAGE__.'::Exception' => {
        description=>'IO exception'
    }
);
use strict;
use warnings;

1;
